// RouteSenseBanner is retired — DryRoutes was removed from Rejn.
// Keeping an inert export so any lingering imports do not break the build.
export function RouteSenseBanner(_props: unknown) {
  return null;
}
